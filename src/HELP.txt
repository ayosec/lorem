lorem: random text generator.

Usage: lorem [ -c | -r | -f FILE | -w WIDTH ] COUNT

  -c      Change COUNT to characters instead of words.
  -r      Use random ASCII letters, instead of text from lipsum text.
  -f      Load words from FILE.
  -w      Wrap content to WIDTH.

  COUNT   Number of words or characters to print (100 by default).
